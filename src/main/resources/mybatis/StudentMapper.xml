<?xml version="1.0" encoding="UTF-8" ?>
        <!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
                "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.as.mappermybatis.StudentMapper">
<resultMap id="baseResultMap" type="Student">
    <id column="stu_id" property="stuId" jdbcType="INTEGER" />
    <result column="stu_name" property="stuName" jdbcType="VARCHAR"/>
    <result column="stu_age" property="stuAge" jdbcType="INTEGER"/>
    <result column="stu_sex" property="stuSex" jdbcType="VARCHAR"/>
    <result column="stu_bir" property="stuBirthday" jdbcType="TIMESTAMP" />
</resultMap>
<sql id="base_col_list">stu_id, stu_name, stu_age, stu_sex, stu_bir</sql>
<select id="selectAll" resultMap="baseResultMap">
    select
    <include refid="base_col_list" />
    from student
</select>
<insert id="insertOne" parameterType="Student">insert into student(stu_name, stu_age, stu_sex, stu_birthday) values(#{stuName}, #{stuAge}, #{stuSex}, #{stuBirthday})
    </insert>
<update id="updateOne" parameterType="Student">
        update student
        set stu_name=#{stuName}, stu_sex=#{stuSex},
        stu_age=#{stuAge}, stu_birthday=#{stuBirthday}
        where stu_id = #{stuId}
    </update>
<delete id="deleteById" parameterType="java.lang.Integer">
        delete from student where stu_id = #{stuId}
       </delete>
</mapper>